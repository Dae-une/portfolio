import { style } from '@vanilla-extract/css';

export const ProgressBar = style({
  paddingTop: '5.7rem',
  display: 'grid',
});

export const Progress = style({
  height: '4px',
  background: 'yellow',
});
